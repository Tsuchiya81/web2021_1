<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>一覧表</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name %></td>
            <td><%= row.price %></td>
            <td><%= row.name2 %></td>
        </tr>
      <% } %>
  </table>
        <a href="/">トップへ戻る</a><br>
        <a href="/public/insert.html">商品を追加</a>
</body>
</html>