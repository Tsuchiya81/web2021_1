<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>絞り込み</title>
  <style>
  body {
    font-family:serif;
  </style>
</head>
<body>
  <h2>絞り込み結果</h2>
  <form action="/detail">
    <table border="1">
      <tr>
        <th>ID　</th>
        <th>商品名　</th>
        <th>カロリー(kcal)　</th>
        <th>容量　</th>
        <th>メーカー名　</th>
        <% for( let row of data ) { %>
          <tr>
              <td><label><input type="radio" name="id" value="<%= row.id %>"><%= row.id %></label></td>
              <td><%= row.name %></td>
              <td><%= row.cal %></td>
              <td><%= row.size %></td>
              <td><%= row.name2 %></td>
          </tr>
        <% } %>
      </tr>
    </table>
    <input type="submit" value="選択した商品の詳細を表示">
  </form>
        <a href="/makerselect">絞り込みメーカーを変える</a><br>
        <a href="/insele">商品を追加</a><br>
        <a href="/desele">商品を削除</a><br>
        <a href="/top">一覧へ戻る</a><br>
        
</body>
</html>